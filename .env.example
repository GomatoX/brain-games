# ──────────────────────────────────────────────
# Brain Games Platform – Environment Variables
# ──────────────────────────────────────────────
# Copy to .env and fill in values before deploying.

# ─── Directus (Backend) ───
SECRET=change-me-random-secret-key
KEY=change-me-random-key
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me
PUBLIC_URL=https://api.example.com
CORS_ORIGIN=https://example.com,https://games.example.com

# ─── Frontend (Game Engine) ───
VITE_API_URL=https://api.example.com

# ─── Landing / Dashboard ───
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_FRONTEND_URL=https://games.example.com
DIRECTUS_ADMIN_TOKEN=your-directus-admin-token
GOOGLE_AI_API_KEY=your-google-ai-key

# ─── White-Label Settings ───
# Set to "whitelabel" to hide the marketing landing page
# and show only the dashboard + games.
NEXT_PUBLIC_MODE=saas

# Custom platform name (shown in sidebar, login page, browser title)
NEXT_PUBLIC_PLATFORM_NAME=Rustycogs.io

# Optional: custom logo path (relative or absolute URL)
NEXT_PUBLIC_PLATFORM_LOGO=

# Primary accent color (hex)
NEXT_PUBLIC_PLATFORM_ACCENT=#c25e40

# Set to "true" to redirect / → /dashboard (hides marketing landing page)
NEXT_PUBLIC_HIDE_LANDING=false

# Set to "true" to disable self-registration (/register → /login redirect)
NEXT_PUBLIC_HIDE_REGISTER=false
